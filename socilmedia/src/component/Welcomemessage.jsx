const Welcomemessage =({postclick})=>{
   return(<center > <h1>There is no post to show</h1>
    <button onClick={postclick}>Post From Dumy API to show</button></center>)
}
export default Welcomemessage